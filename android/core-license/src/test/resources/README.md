Test resources:
- license-vectors.json — copy of the shared cross-platform conformance
  vectors (../../license/vectors.json at the repo root). Keep in sync.
- VOR_LICENSE_PUBLIC_KEY.txt — the committed DEV public key
  (../../license/keys/dev/). Development/test only; release builds embed
  the production public key via build config.
