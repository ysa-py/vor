<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VorLicenseManager">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <!-- android:name is a MANIFEST-MERGER KEY ONLY (no runtime
                 effect): it lets the issuer build variant remove this
                 launcher intent-filter via tools:node="remove" so the
                 biometric-gated IssuerActivity becomes the single entry
                 point of the ISSUER apk. The public verifier build ships
                 exactly this filter, byte-identical behavior. -->
            <intent-filter android:name="launcher">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Accept pasted/shared license tokens from other apps. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>
</manifest>
