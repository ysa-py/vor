# مشارکت در MSN-GUARD

ممنون که می‌خواهید برنامه MSN-GUARD را بهتر کنید. این پروژه یک کلاینت VPN اندروید با هستهٔ Rust است و هر دو لایه در همین مخزن نگهداری می‌شوند.

## پیش از ثبت issue

۱. اول به آخرین بیلد به‌روزرسانی کنید.
۲. سری به [issue های باز](https://github.com/mbm110/MSN-GUARD/issues) بزنید تا تکراری نشود.
۳. اگر گزارش مربوط به اتصال است، نام پروتکل و نسخهٔ اندروید و معماری دستگاه و لاگ سانسورشدهٔ اتصال را بنویسید.
۴. هرگز کلید یا هویت provisioning یا endpoint خصوصی یا اسکن کامل IP یا ضبط ترافیک را در issue عمومی نگذارید.

## آماده‌سازی محیط توسعه

ابزارهای لازم: محیط Android Studio، بستهٔ SDK 36، نسخهٔ NDK `26.3.11579264`، ابزار CMake `3.22.1`، جاوای JDK 17، زبان Rust نسخهٔ stable و `cargo-ndk`. جزئیاتش در [راهنمای ساخت](docs/build.md) آمده است.

## قواعد تغییر

اینها از اشتباهات واقعی همین پروژه بیرون آمده‌اند، نه از سلیقه:

- **شاهد بیاورید.** هر تغییری در رفتار شبکه باید با لاگ یا اندازه‌گیری یا خروجی probe پشتیبانی شود. ترتیب گیت‌وی‌ها و انتخاب پروتکل و بودجه‌های زمانی همه اندازه‌گیری‌شده‌اند؛ با حدس عوضشان نکنید.
- **ایمنی VPN را نشکنید.** سوکت‌های ترابری هسته باید با `VpnService.protect()` از TUN محافظت شوند. بستهٔ خود برنامه هم باید در `addDisallowedApplication` بماند، وگرنه حلقهٔ مسیریابی می‌سازد.
- تا وقتی هسته آمادگی واقعی تونل را اعلام نکرده، حالت **Connected** را نشان ندهید.
- رابط کاربری بومی و کم‌وابستگی بماند، مگر یک وابستگی واقعاً لازم باشد.
- لاگ به‌دردبخور اضافه کنید، ولی مواد حساس را به‌صورت پیش‌فرض سانسور کنید.
- حالت پروکسی حذف شده است. کدی که SOCKS را برای مصرف بیرونی باز کند اضافه نکنید؛ هدف پروژه تونل کردن کل دستگاه است.

## فرستادن pull request

۱. از آخرین `master` شروع کنید.
۲. هر PR روی یک مسئله متمرکز باشد.
۳. در توضیحات بنویسید کاربر چه تغییری می‌بیند و چه چیزی تست شده.
۴. پیش از درخواست بازبینی، بیلد Gradle مربوطه را اجرا کنید.
۵. برای تغییرات رابط اسکرین‌شات بگذارید و برای تغییرات اتصال لاگ سانسورشده.

## سبک commit

موضوع کوتاه و امری باشد:

```text
fix: keep settings back button below status bar
feat: add armv7 debug release build
docs: explain the Rust core boundary
```

## آیین رفتار

محترم و مشخص و سازنده باشید. گزارش و بازبینی روی رفتار و شاهد تمرکز کند، نه روی افراد.

## گزارش امنیتی

مسائل امنیتی از [SECURITY.md](SECURITY.md) پیروی می‌کنند، نه از issue عمومی.
